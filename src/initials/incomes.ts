import { ESTABLISHMENTS } from "../c";
import { Establishment, HeroProps } from "../t";

export function industries(pool: Establishment[], hero: HeroProps) {
  for (let i = 0; i < pool.length; i++) {}
}
